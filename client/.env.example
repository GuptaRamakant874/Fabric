# Frontend deployment config
# For a separate backend host, set this to the backend origin, for example:
# VITE_API_URL=https://api.yourdomain.com
#
# For a same-host deployment where the backend is mounted at /api, use:
# VITE_API_URL=/api
VITE_API_URL=http://localhost:5000/api
